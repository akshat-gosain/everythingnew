#include <vector>
#include <queue>
#include <iostream>
using namespace std;

vector<int> solve(int n, int e, vector< pair<int,int> > edges) {
	vector< vector<int> > adj(n);

	vector<int> topo_sort;

	return topo_sort;
}
